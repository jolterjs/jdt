# Support

Use GitHub issues for bug reports and feature requests.

Before opening an issue:

- Search existing issues.
- Try the latest released version.
- Include exact commands and versions.

For questions about plugin development, include the relevant `plugin.json`,
`src/plugin.ts` or `src/plugin.js`, and any generated `dist/plugin.release.json`
that helps explain the problem.
