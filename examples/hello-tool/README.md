# Hello Tool Plugin

This plugin is a deterministic fixture for JDT. It does not perform network
requests during tests and always resolves `hello-tool@1.x` to `1.2.3`.

From this directory you can install and run it locally:

```sh
bun install
bun run run:list
bun run run:resolve
```
